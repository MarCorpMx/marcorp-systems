import { DisableWhileLoading } from './disable-while-loading';

describe('DisableWhileLoading', () => {
  it('should create an instance', () => {
    const directive = new DisableWhileLoading();
    expect(directive).toBeTruthy();
  });
});
